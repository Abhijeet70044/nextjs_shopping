import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Remove appDir since it's part of Next.js 13+ experimental features
};

export default nextConfig;
